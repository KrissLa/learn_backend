name: CI

on:
  push:
    branches:
      - main

jobs:
  lint:
    name: Run linter
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Run lint
        run: make lint_in_docker
